---
applications:
  - name: go-app-demo
    memory: 128M
    instances: 1
    buildpacks:
      - go_buildpack
    # ISSUE 1: VERSION still contains -SNAPSHOT which should be stripped
    env:
      VERSION: 1.189.0-SNAPSHOT
    # ISSUE 2: No health check endpoint configured
    # health-check-type: http
    # health-check-http-endpoint: /health
    
    # ISSUE 3: No route configuration - using default random route
    # routes:
    #   - route: go-app-demo.example.com
    
    # ISSUE 4: Missing resource limits and scaling configuration
    # disk_quota: 256M
    
    # ISSUE 5: No environment-specific configuration (dev/staging/prod)
